name: flutter_chat_list_view_example
description: A new Flutter project.

publish_to: 'none'

environment:
  sdk: '>=2.12.0-0 <3.0.0'
  flutter: '>=1.13.8'

dependencies:
  flutter:
    sdk: flutter
  flutter_chat_list_view:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter